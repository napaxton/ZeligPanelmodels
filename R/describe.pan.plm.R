describe.pan.plm <- function(...){
    category <- "continuous"
    description <- "Econometric-style Linear Estimation for Panel Data"
    authors <- c("Nathan A. Paxton")
    year <- c(2011, 2012)
# 	package <- list(name       = "plm",
#                     version    = "1.2-7"
#                 	 )
# 	parameters <- list(mu="mu") 
# 	parameters$mu <- list(equations = c(1,1),
# 	                    tagsAllowed = FALSE,
# 	                    depVar = TRUE,
# 	                    expVar = TRUE
# 	                 	)   
	
	list(category = category, 
		 authors = authors, 
		 year = year, 
		 text = description
#  		 package = package
# 		 parameters = parameters
		)
}
                          
